export default function Pagination(){
    return(<>
    <div className="flex items-center gap-4  justify-end">
{/* {Array.from({length:5},(_,i)=>i+1).map((val)=><span key={val}><button className="px-4 py-2 ring-2 ring-slate-300">{val}</button></span>)} */}
    </div>
    </>)
}